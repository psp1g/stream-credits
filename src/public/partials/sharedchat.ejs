<% if (data.stream && data.stream.specialGuests && data.stream.specialGuests.length > 0) { %>
<div class="segment leftCol">
    <div class="header midCaps">SPECIAL GUESTS</div>
    
        <% data.stream.specialGuests.forEach((title, index) => { %>
            <div class="bigText veryCloseLineHeight mb-4"><%= title %></div>
        <% }); %>
        
        
    </div>
<% } %>