<div class="segment rightCol">
    <div class="header midCaps">CHAPTERS</div>
    
    <% if (data.stream && data.stream.titleHistory && data.stream.titleHistory.length > 0) { %>
        <% data.stream.titleHistory.forEach((title, index) => { %>
            <div class="midCaps veryCloseLineHeight mb-4"><%= title %></div>
        <% }); %>
    <% } %>
    

</div>